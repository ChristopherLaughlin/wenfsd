{
  "name": "wenfsd",
  "version": "0.1.0",
  "private": true,
  "description": "wenFSD — root entry so hosts (Railway/Render) run the Node backend in server/, which also serves the static frontend at the repo root.",
  "engines": { "node": ">=18.17" },
  "scripts": {
    "postinstall": "npm --prefix server install",
    "start": "node server/src/server.js"
  }
}
